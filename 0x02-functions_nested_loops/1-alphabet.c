#include "main.h"

/**
 * main-Entry point
 * printing alphabet in lowe case
 * Return: 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');

	return (0);
}
